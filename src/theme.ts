import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgb(24, 130, 156)",
    },
  },
  shape: {
    borderRadius: 12,
  },
});

export default theme;
